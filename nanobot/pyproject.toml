[project]
name = "nanobot"
version = "0.1.0"
description = "Repo-local nanobot setup"
requires-python = ">=3.14"
dependencies = [
  "nanobot-ai",
  "lms-mcp",
  "nanobot-webchat",
]

[tool.uv.sources]
lms-mcp = { workspace = true }
nanobot-webchat = { workspace = true }
